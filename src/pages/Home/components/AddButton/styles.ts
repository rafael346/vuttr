
import styled from "styled-components";

export const Button = styled.button`
  justify-content: center;
  align-items: center;
  width:  11rem;
  height: 3rem;
  background: var(--blue);
  color: var(--white);
  border-radius: 5px;
  border: none;

`
